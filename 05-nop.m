#include "monty.h"

/* nop opcode implementation */
void nop(stack_t **stack, unsigned int line_number)
{
    (void)stack; /* Prevent unused parameter warning */
    (void)line_number; /* Prevent unused parameter warning */
}
